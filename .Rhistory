focal.tip <- tree$tip.label[i]
cur.tip <- tree$tip.label[j]
plot(tree)
edgelabels(text=round(tree$edge.length,4),frame="none")
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
tree$tip.label
i<-3
j<-4
focal.tip <- tree$tip.label[i]
cur.tip <- tree$tip.label[j]
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
SeqDef <- function(tree, df, data.col, invert, scale){
# The above line stops the code if the tree and data differ in length
if(length(tree$tip.label) != nrow(df)){
stop("Tree and Data should have same Length")
}
# Tree depth (td) is calculated above
td <- max(branching.times(tree))
## TODO
# use cophenetic.phylo from APE to make a matrix of distances ∆
# then divide the matrix by two to get the distance to the MRCA ∆
# the divide the values by the tree depth that you calculate above ∆
# then do 1 - the values in the matrix ∆
dist.matrix <- cophenetic.phylo(tree)
dist.prop <- 1- ((dist.matrix/2) / td)
# At this point you should have a matrix that you can look up
# the values that you need below at the second TODO
for(i in 1:length(tree$tip.label)){
focal.tip <- tree$tip.label[i]
x <- c()
for(j in 1:length(tree$tip.label)){
cur.tip <- tree$tip.label[j]
# The first seven lines below will go away instead
# of calculating every time, we will just look up the
# values that we need for any particular pairing
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
}
}
if(invert){
df$syn.imp.vals[df$species == focal.tip] <- 1 - mean(x)
}else{
df$syn.imp.vals[df$species == focal.tip] <- mean(x)
}
if(scale){
results <- (df$syn.imp.vals - min(df$syn.imp.vals)) /
(max(df$syn.imp.vals) -min(df$syn.imp.vals))
}else{
results <- df$syn.imp.vals
}
return(results)
}
#### Loading required packages.####
library(ape)
library(phytools)
#### Generating tree, importance and data values ####
taxaN <- 100
#### Loading required packages.####
library(ape)
library(phytools)
tree <- pbtree(n=taxaN)
plot(tree)
edgelabels(text=round(tree$edge.length,4),frame="none")
#### Generating tree, importance and data values ####
taxaN <- 50
tree <- pbtree(n=taxaN)
plot(tree)
### inputs for the table
imp <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2)
### inputs for the table
imp <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
imp <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
data <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
df <- data.frame(tree$tip.label,  imp,  data)
colnames(df)[1] <- "species"
View(df)
df <- SeqDef(tree=tree, df=df, data.col=3, invert=T)
# Get syn.imp.vals
df <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
df
# Get syn.imp.vals
df <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= T)
df <- data.frame(tree$tip.label,  imp,  data)
colnames(df)[1] <- "species"
df1 <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= T)
df1
length(df1)
matrix(NA, 100, 2)
df1 <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
df1
# Get syn.dat.vals
dist.prop
# Get syn.dat.vals
dist.matrix
# Get syn.imp.vals
df1 <- SeqDef(tree=tree, df=df, data.col=3, invert=F, scale= F)
df1
df1 <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
df1
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree)
edgelabels(text=round(tree$edge.length,4),frame="none")
# inputs for the table
imp <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
data <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
df <- data.frame(tree$tip.label,  imp,  data)
colnames(df)[1] <- "species"
edgelabels(text=round(tree$edge.length,3),frame="none")
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree)
edgelabels(text=round(tree$edge.length,3),frame="none")
# inputs for the table
imp <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
data <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree)
edgelabels(text=round(tree$edge.length,3),frame="none")
# inputs for the table
imp <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
data <- sample(x=c(0,1), size=taxaN , replace=T, prob=c(0.8,0.2))
df <- data.frame(tree$tip.label,  imp,  data)
colnames(df)[1] <- "species"
j <- 1
i <- 4
td <- max(branching.times(tree))
dist.matrix <- cophenetic.phylo(tree)
dist.prop <- 1- ((dist.matrix/2) / td)
cur.tip <- tree$tip.label[j]
focal.tip <- tree$tip.label[i]
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip]
i <- 1
j <- 2
focal.tip <- tree$tip.label[i]
cur.tip <- tree$tip.label[j]
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
x <- c()
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
data.col <- 2
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
x
View(df)
cur.tip
data.col
df[ ,data.col]
cur.tip <- 3
focal.tip <- 5
cur.tip <- tree$tip.label[j]
focal.tip <- tree$tip.label[i]
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
x
cur.tip
cur.tip
cut.tip <- 7
ur.tip <- tree$tip.label[j]
cur.tip <- 5
cur.tip <- tree$tip.label[j]
j <- 5
cur.tip <- tree$tip.label[j]
i <- 4
focal.tip <- tree$tip.label[i]
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
x
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip]
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
df1
SeqDef <- function(tree, df, data.col, invert, scale){
# The above line stops the code if the tree and data differ in length
if(length(tree$tip.label) != nrow(df)){
stop("Tree and Data should have same Length")
}
# Tree depth (td) is calculated above
td <- max(branching.times(tree))
## TODO
# use cophenetic.phylo from APE to make a matrix of distances ∆
# then divide the matrix by two to get the distance to the MRCA ∆
# the divide the values by the tree depth that you calculate above ∆
# then do 1 - the values in the matrix ∆
dist.matrix <- cophenetic.phylo(tree)
dist.prop <- 1- ((dist.matrix/2) / td)
# At this point you should have a matrix that you can look up
# the values that you need below at the second TODO
for(i in 1:length(tree$tip.label)){
focal.tip <- tree$tip.label[i]
x <- c()
for(j in 1:length(tree$tip.label)){
cur.tip <- tree$tip.label[j]
# The first seven lines below will go away instead
# of calculating every time, we will just look up the
# values that we need for any particular pairing
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
}
}
if(invert){
df$syn.imp.vals[df$species == focal.tip] <- 1 - mean(x)
}else{
df$syn.imp.vals[df$species == focal.tip] <- mean(x)
}
if(scale){
results <- (df$syn.imp.vals - min(df$syn.imp.vals)) /
(max(df$syn.imp.vals) -min(df$syn.imp.vals))
}else{
results <- df$syn.imp.vals
}
return(results)
}
PlotSeqDef <- function(tree, df, data.col){
tree$edge.length <- tree$edge.length/ max(branching.times(tree))
plot(tree, cex=.7)
tiplabels(text=df[, data.col], adj=-1.5, frame="none", col="red",cex=.7)
tiplabels(text=as.character(df[, 4]), offset=-.1, cex=.7,frame="none", col="blue")
}
#### Loading required packages.####
library(ape)
library(phytools)
# Here we pull in the functions that we have written
source("functions.R")
setwd("~/Downloads/github/seqdef")
#### Generating tree, importance and data values ####
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree)
edgelabels(text=round(tree$edge.length,3),frame="none")
branching.times(tree)
max(branching.times(tree))
td
td <- max(branching.times(tree))
length(tree$tip.label))
tree$tip.label
length(tree$tip.label)
focal.tip <- tree$tip.label[i]
i <- 1
j <-3
focal.tip <- tree$tip.label[i]
focal.tip
cur.tip <- tree$tip.label[j]
x <- c()
x <- c(x,
(1 - ((fastDist(tree, focal.tip, cur.tip)) / 2) / td)*
df[,data.col][df$species == cur.tip])
data.col = 2
imp <- c(0,0,0,0,1)
data <- c(0,0,0,0,1)
df <- data.frame(tree$tip.label,  imp,  data)
colnames(df)[1] <- "species"
# Tree depth (td) is calculated above
td <- max(branching.times(tree))
dist.matrix <- cophenetic.phylo(tree)
dist.prop <- 1- ((dist.matrix/2) / td)
dist.matrix
dist.prop
dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
x <- c(x, dist.prop[row.names(dist.prop) == cur.tip,
colnames(dist.prop) == focal.tip]
* df[ ,data.col][df[,1] == cur.tip])
x
df$syn.imp.vals[df$species == focal.tip] <- 1 - mean(x)
df$syn.imp.vals
df$syn.imp.vals[df$species == focal.tip] <- mean(x)
df$syn.imp.vals
results <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
results
results <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= T)
results
results <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
results
results
if(invert){
df$syn.imp.vals[df$species == focal.tip] <- 1 - mean(x)
}else{
df$syn.imp.vals[df$species == focal.tip] <- mean(x)
}
# line 32 stores the mean value of x that we got from earlier inside a
# data.frame as "synthetic importance values" (syn.imp.vals)
# when the species name is
# the same as the focal.tip we were comparing our cur.tip with.
}
if(scale){
results <- (df$syn.imp.vals - min(df$syn.imp.vals)) /
(max(df$syn.imp.vals) -min(df$syn.imp.vals))
}else{
results <- df$syn.imp.vals
}
return(results)
if(invert){
df$syn.imp.vals[df$species == focal.tip] <- 1 - mean(x)
}else{
df$syn.imp.vals[df$species == focal.tip] <- mean(x)
}
# line 32 stores the mean value of x that we got from earlier inside a
# data.frame as "synthetic importance values" (syn.imp.vals)
# when the species name is
# the same as the focal.tip we were comparing our cur.tip with.
}
if(scale){
results <- (df$syn.imp.vals - min(df$syn.imp.vals)) /
(max(df$syn.imp.vals) -min(df$syn.imp.vals))
}else{
results <- df$syn.imp.vals
if(invert){
df$syn.imp.vals[df$species == focal.tip] <- 1 - mean(x)
}else{
df$syn.imp.vals[df$species == focal.tip] <- mean(x)
}
# line 32 stores the mean value of x that we got from earlier inside a
# data.frame as "synthetic importance values" (syn.imp.vals)
# when the species name is
# the same as the focal.tip we were comparing our cur.tip with.
}
if(scale){
results <- (df$syn.imp.vals - min(df$syn.imp.vals)) /
(max(df$syn.imp.vals) -min(df$syn.imp.vals))
}else{
results <- df$syn.imp.vals
}
results
syn.val.frame
syn.imp.table$syn.imp.vals
final.df <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
final <- SeqDef(tree=tree, df=df, data.col=3, invert=T, scale= F)
results <- matrix(NA, 100, 2)
colnames(results) <- c("mean.raw","mean.syn")
for(i in 1:100){
print(i)
set.seed(i)
taxaN <- 50
tree <- pbtree(n=taxaN)
imp <- runif(taxaN)
df <- data.frame(tree$tip.label,  imp)
colnames(df)[1] <- "species"
synvals <- SeqDef(tree=tree, df=df, data.col=2, invert=T, scale=F)
results[i,2] <- mean(synvals)
results[i,1] <- mean(df[,2])
}
synvals
results[i,2] <- mean(synvals)
results[i,1] <- mean(df[,2])
results[i,2]
plot(tree, cex=.7)
tiplabels(text=df[, data.col], adj=-1.5, frame="none", col="red",cex=.7)
tiplabels(text=as.character(df[, 4]), offset=-.1, cex=.7,frame="none", col="blue")
}
{
tree$edge.length <- tree$edge.length/ max(branching.times(tree))
plot(tree, cex=.7)
tiplabels(text=df[, data.col], adj=-1.5, frame="none", col="red",cex=.7)
tiplabels(text=as.character(df[, 4]), offset=-.1, cex=.7,frame="none", col="blue")
}
df[, 4]
df[ , 4]
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.7)
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.7)
plot(tree, cex=0.8)
col= tree$Nnode
tree <- pbtree(n=taxaN)
plot(tree, cex=0.8)
col = tree$Nnode
col = "tree$Nnode"
col = "tree$Nnode"
col = tree$tip.label
col = "tree$tip.label"
plot(tree, cex=0.8)
col = "tree$tip.label"
plot(tree, cex=0.8)
col = "tree$tip.label"
col = "blue"
tiplabels(adj=-1.5, frame="none", col="red",cex=.7)
tiplabels(text= tree$tip.label, adj=-1.5, frame="none", col="red",cex=.7)
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.8)
tiplabels(text= tree$tip.label, adj=-1.5, frame="none", col="red",cex=.7)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="none")
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red")
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red")
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.9)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.9)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.8)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="black", col = "red", cex=0.8)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="blue", col = "red", cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="circle", col = "red", cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.8)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.8)
plot(tree, cex=0.8)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.7)
plot(tree, cex=0.9, adj= 1.5)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.7)
plot(tree, cex=0.9, adj= 2.0)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.7)
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.9, adj= 2.0)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.7)
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.9, adj= 1.5)
edgelabels(text=round(tree$edge.length,3),frame="rect", col = "red", cex=0.7)
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.9, adj= 1.5)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
taxaN <- 10
tree <- pbtree(n=taxaN)
plot(tree, cex=0.9, adj= 1.5)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex=0.9, adj= 1.5, main= "Tree")
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tree$edge.length,3
tree$edge.length
text=round(tree$edge.length,3
text=round(tree$edge.length,3)
??edgelabels
tree <- pbtree(n=taxaN)
plot(tree, cex=0.9, main= "Tree", offset= 3)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex=0.9, main= "Tree", offset= -7)
plot(tree, cex=0.9, main= "Tree", offset= -7)
plot(tree, cex=0.9, main= "Tree", offset= T)
plot(tree, cex=0.9, main= "Tree")
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tiplabels(text= tree$tip.label, offset = 5)
plot(tree, cex=0.9, main= "Tree")
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tiplabels(text= tree$tip.label, offset = 100)
offset
help(offset)
??offset
plot(tree, cex=0.9, main= "Tree", label.offset = 4)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tiplabels(text= tree$tip.label, col= "blue" )
plot(tree, cex=0.9, main= "Tree", label.offset = 2)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tiplabels(text= tree$tip.label, col= "blue" )
plot(tree, cex=0.9, main= "Tree", label.offset = 1)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tiplabels(text= tree$tip.label, col= "blue" )
plot(tree, cex=0.9, main= "Tree", label.offset = 1, adj= 1)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
tiplabels(text= tree$tip.label, col= "blue" )
plot(tree, cex=0.9, main= "Tree", label.offset = 1, adj= 1)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex=0.9, main= "Tree", label.offset = 1, adj= 0)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex=0.9, main= "Tree", label.offset = 1, adj= -4)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex=0.9, main= "Tree", label.offset = 1, adj= -15)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex= 1, main= "Tree", label.offset = 1, adj= -25)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex= 1, main= "Tree", label.offset = 1, adj= -20)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex= 1, main= "Tree", offset = 1, adj= -20)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex= 1, main= "Tree", offset = 1, adj= -20)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex= 1, main= "Tree", offset = 1, adj= 4)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.7)
plot(tree, cex= 1, main= "Tree", offset = 1, adj= 4)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.8)
plot(tree, cex= 1, main= "Tree", offset = 1, adj= 2)
edgelabels(text=round(tree$edge.length,3),frame="none", col = "red", cex=0.8)
